# MD Editor

## Install

### Next.js

```sh
yarn create next-app momohiki --typescript
```

```diff
      "build": "next build",
+     "export": "next build && next export",
      "start": "next start",
```
